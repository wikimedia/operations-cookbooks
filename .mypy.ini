[mypy]
ignore_missing_imports = True
exclude = (?x)(
    ^build/.*$           # build directory
    | ^.tox/.*$          # tox venvs
    | ^cookbooks/sre/.*$ # SRE related cookbooks, not using mypy yet
    | ^tests/.*
  )
